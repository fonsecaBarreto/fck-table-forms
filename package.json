{
  "name": "fck-brasil-location-selector",
  "version": "1.0.0",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "license": "MIT",
  "private": false,
  "scripts": {
    "example:1": "ts-node-dev -r tsconfig-paths/register ./examples/test01.ts ",
    "test": "jest --passWithNoTests --runInBand --no-cache --watch --silent",
    "build": "rimraf lib/* && tsc -p tsconfig-build.json",
    "build:watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.1",
    "copyfiles": "^2.4.1",
    "jest": "^27.4.5",
    "rimraf": "^3.0.2",
    "ts-jest": "^27.1.2",
    "ts-node-dev": "^1.1.8",
    "tsconfig-paths": "^3.12.0",
    "typescript": "^4.5.4"
  },
  "peerDependencies": {
    "typescript": ">=4.5.3"
  },
  "files": [
    "/lib"
  ],
  "dependencies": {
    "axios": "^0.26.0"
  }
}
